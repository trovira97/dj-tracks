# DJ Tracks — Music Downloader
# Requirements · Python 3.10+

# ── GUI ───────────────────────────────────────────────────────────────────────
customtkinter>=5.2.0
Pillow>=10.0.0

# ── Audio download ────────────────────────────────────────────────────────────
# Dual engine: spotdl is preferred for Spotify/Apple Music (exact match via
# Spotify API + multi-provider retry), yt-dlp is preferred for SoundCloud/
# Bandcamp (direct URL download) and is the automatic fallback when spotdl
# can't deliver.  Both are required.
yt-dlp>=2024.1.1
spotdl>=4.2.0

# ── Music platform APIs ───────────────────────────────────────────────────────
spotipy>=2.23.0
soundcloud-v2>=1.3.0

# ── Metadata ──────────────────────────────────────────────────────────────────
mutagen>=1.47.0

# ── Network ───────────────────────────────────────────────────────────────────
requests>=2.31.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-slugify>=8.0.0
platformdirs>=4.0.0

# ── Optional UX features (gracefully degraded if missing) ─────────────────────
tkinterdnd2>=0.5.0       # drag-and-drop URLs onto the search bar
qrcode[pil]>=7.4.0       # QR codes in the donation dialog
pygame>=2.5.0            # in-app audio preview / play button

# ── DJ metadata enrichment (BPM / key / Camelot) ──────────────────────────────
rapidfuzz>=3.0.0         # better GetSongBPM title matching (has a fallback)
# librosa>=0.10.0        # OPTIONAL local BPM/key analysis — heavy (~numpy/scipy);
# numpy>=1.24.0          # install manually only if you want offline DJ analysis.
